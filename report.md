# 스마트모빌리티공학실험2 최종 프로젝트 결과 보고서

| 학번 | 이름 |
|------|------|
| 12243756 | 김대현 |

## 1. 모티베이션 & 인트로

### 1.1 문제 정의 및 기존 물리·기하학적 모델링의 한계
본 과제는 3GPP Indoor Factory Dense-High (InF-DH) 시나리오를 가정하여, 18개 기지국(BS)에서 수집된 RTT(Round-Trip Time) 측정값을 기반으로 사용자의 2D 위치 좌표 $(x, y)$를 추정하는 것을 목표로 한다. 학습 데이터는 700명이며, 최종 평가는 정답이 가려진 Hidden Test Set 300명에 대해 수행되므로 일반화(Generalization) 성능 확보가 필수적이다. 

이 문제의 가장 큰 난제는 공장 내부의 철제 구조물과 복잡한 환경으로 인해 발생하는 극단적인 다중 경로 페이딩(Multipath Fading)과 비가시거리(NLOS, Non-Line-of-Sight) 오차다. 중간고사까지의 실험에서는 센서 간 일치도(Sensor Agreement)와 IRWLS(반복 재가중 최소제곱법) 등을 활용한 최적화 알고리즘(SAFL v7)을 설계하여 접근했다. 그러나 엄밀한 삼변측량이나 수식 기반의 최적화 기법은 특정 센서에 수십 미터 단위의 비선형적인 양(+)의 바이어스가 무작위로 개입하는 상황에서, 인간이 직관으로 설정한 하드 클리핑(Hard Clipping)이나 잔차 기반 가중치 조절 수식만으로는 위치 왜곡을 방어하는 데 이론적 한계가 존재했다. 정확도를 한 단계 더 끌어올리기 위해서는 인간의 휴리스틱을 배제하고, 데이터 자체에 내재된 복잡한 패턴을 추출하는 새로운 패러다임이 요구되었다.

### 1.2 딥러닝 패러다임 전환과 Self-Attention 도입의 당위성
본 프로젝트는 수식 기반의 물리적 추정 방식을 탈피하고 데이터 주도형(Data-driven) 딥러닝 모델링으로 접근 방식을 전면 전환하였다. 

그러나 단순히 18개의 측정 거리를 1차원 배열로 평탄화(Flatten)하여 다층 퍼셉트론(MLP)에 주입하는 일반적인 딥러닝 방식은 센서들 사이의 기하학적 유기성을 훼손한다. 이에 본 연구는 18개 기지국의 데이터를 하나의 연속된 공간적 패턴(Spatial Fingerprint)으로 해석하고, 자연어 처리 영역에서 주로 활용되는 Transformer의 Self-Attention 메커니즘을 위치 측위 도메인에 독창적으로 이식하였다. 

18개의 기지국 각각을 독립적인 토큰(Token)으로 취급하여 Self-Attention 레이어에 통과시킬 경우, 신경망은 18개 기지국 간의 거리 관계를 상호 교차 검증하게 된다. 즉, 특정 기지국의 측정값이 NLOS로 인해 엉뚱하게 튀더라도, 어텐션 메커니즘이 다른 정상적인 측정값들과의 기하학적 모순을 스스로 감지하고 해당 기지국의 가중치(Attention Score)를 동적으로 억제한다. 

### 1.3 DSAN (Dynamic Sensor Attention Network) 알고리즘 개요
전체 파이프라인은 다음과 같이 크게 세 단계로 구성된다.

1. 도메인 지식 기반 전처리: 공간의 물리적 Bounding Box를 계산하여 신호가 도달할 수 없는 결측치 및 이상치를 최대 유효 거리 $D_{max}$로 치환한다. 
2. Self-Attention 추론망: 18개 기지국의 $(X, Y, RTT)$ 좌표·거리 쌍을 3차원 특성 벡터 시퀀스로 구성한 뒤, 어텐션 기반 인코더를 통해 동적 가중치를 산출하여 좌표를 회귀(Regression) 추정한다.
3. Huber 손실 및 추론 가속화: 학습 시 극단적 이상치에 강건한 Huber Loss를 적용하고, 추론 단계에서는 단일 순전파 연산과 전역 변수 메모리 적재 기법을 활용하여 제한 시간(10분) 문제를 해결한다.

---

## 2. 알고리즘 설명

본 프로젝트에서 제안하는 DSAN 알고리즘은 인간의 물리적 직관을 주입하는 '도메인 지식 기반 전처리' 단계와, 노이즈를 동적으로 억제하는 'Self-Attention 추론' 단계로 나뉜다.

### 2.1 Stage 1: 기하학적 도메인 지식을 활용한 물리적 한계 필터링
순수 딥러닝(End-to-End) 모델은 과제와 같이 700개의 소규모 훈련 데이터만 주어질 때 아주 작은 노이즈에도 예측 공간(Manifold)이 붕괴하는 과적합(Overfitting)에 빠지기 쉽다. 특히, InF-DH 환경에서 통신이 단절되어 결측치(NaN)가 발생하거나 0 이하의 오류 값이 기록될 때, 이를 단순히 0으로 치환(Zero-padding)하면 모델은 이를 "기지국과 물리적으로 밀착해 있다"고 심각하게 오판하게 된다.

Güvenç et al. [3]의 연구에서는 실내 다중 경로 페이딩 환경에서 측정 거리가 양(+)의 방향으로 길어지는 특성을 분석하여 WLS(가중 최소제곱법)의 가중치를 수동으로 낮추는 방식을 제안했다. 본 프로젝트는 해당 논문의 물리적 특성만을 차용하되 이를 수동적 가중치 조절이 아닌 기하학적 Bounding Box 기반의 데이터 전처리 조건으로 명시화하여 차별점을 두었다. 18개 기지국의 배치 좌표를 기준으로 공장 전체 영역을 아우르는 외곽선의 대각선 길이 여유분(1.2배)을 최대 유효 거리($D_{max}$)로 정의하였다.

$$D_{max} = \sqrt{(X_{max}-X_{min})^2 + (Y_{max}-Y_{min})^2} \times 1.2$$

이후 비현실적인 NLOS 값들을 $D_{max}$로 강제 치환하여, 모델에게 기하학적 한계를 명확하게 학습시켰다.

### 2.2 Stage 2: DSAN 구조 설계
전처리를 거친 거리 데이터는 딥러닝 모델의 입력으로 사용된다. 본 알고리즘은 18개의 기지국을 각각 독립적인 의미를 지닌 토큰(Token)으로 취급하는 Transformer 기반의 Self-Attention 구조를 채택하였다. 각 기지국의 정보는 3차원 특성 벡터로 구성된다.

Vaswani et al. [1]은 자연어 처리(NLP) 분야에서 단어 토큰 간의 문맥적 연관성을 파악하기 위해 Self-Attention을 제안하였다. 본 프로젝트는 이를 단순히 차용하는 데 그치지 않고, 18개 기지국 거리 벡터의 상호 모순을 교차 검증(Cross-validation)하는 '공간적 필터링(Spatial Filtering)' 구조로 변형하여 적용하였다. 특정 기지국에 강한 NLOS 바이어스가 발생했을 때, 쿼리(Query)와 키(Key)의 내적 연산을 통해 다른 기지국 정보와의 기하학적 충돌을 감지하고, 모델 스스로 해당 기지국의 어텐션 가중치(Attention Score)를 0에 가깝게 소거(Decay)시킨다.

### 2.3 Stage 3: Huber Loss 기반 과적합 방지 및 학습 안정화
Self-Attention을 통과하여 1차원으로 전개된 은닉 벡터는 MLP 레이어를 거쳐 최종 예측 좌표 $(x, y)$를 출력한다. 과적합을 억제하기 위해 은닉층마다 Batch Normalization과 Dropout(0.2) 기법을 적용하였으며, 위치 회귀 문제의 손실 함수로 평균제곱오차(MSE) 대신 Huber Loss를 채택하였다.

$$L_{\delta}(a) = \begin{cases} \frac{1}{2} a^2 & \text{for } |a| \le \delta \\ \delta (|a| - \frac{1}{2} \delta) & \text{otherwise} \end{cases}$$

Huber [2]의 연구는 전통적인 통계적 추정에서 이상치를 억제하기 위해 해당 손실 함수를 제안하였다. 본 프로젝트는 이를 딥러닝 모델에 직접 도입하여, 어텐션 레이어가 미처 다 걸러내지 못한 수십 미터 단위의 잔여 NLOS 이상치가 역전파 과정에서 모델의 가중치를 파괴하는 것을 막는 강력한 규제(Regularization) 수단으로 적용 방식을 차별화하였다.

---

## 3. Agent AI 활용 및 협업 프로세스

본 프로젝트에서는 기획부터 디버깅에 이르는 전 과정에 걸쳐 Agent AI(Gemini)를 단순한 코드 생성기가 아닌 '데이터 사이언스 어시스턴트'로 적극 활용하였다.

### 3.1 알고리즘 방향성 설계 및 논리적 검증
초기 설계 단계에서 AI와 심도 있는 토의를 진행하였다. 기존 최적화 기법(SAFL v7)이 가진 한계를 프롬프트로 제시하고, 이를 해결할 딥러닝 방법론에 대한 브레인스토밍을 요구했다. AI가 제안한 여러 회귀 알고리즘 중, 본인이 직접 '비선형성 노이즈 극복'이라는 본질적 목적에 가장 부합하는 Self-Attention 기법을 채택하여 핵심 의사결정을 주도하였다.

### 3.2 도메인 지식의 신경망 아키텍처 변환
본인이 설계한 물리적 도메인 지식($D_{max}$ 외곽선 필터링)을 PyTorch 프레임워크 텐서 연산으로 구체화하는 과정에서 AI의 코딩 능력을 활용하였다. 특히 조교의 채점 환경(10분 제한)을 AI에게 주입하여, 추론 시 매번 디스크에서 모델을 불러오지 않도록 싱글톤(Singleton) 패턴 및 역전파 차단 캐싱 로직을 구현하도록 지시하였다.

### 3.3 실행 환경 트러블슈팅 및 성능 평가
Ubuntu 환경에서 Python 3.12 종속성 충돌 및 시스템 디스크 용량 부족 에러 발생 시, 터미널 에러 로그를 AI에게 제공하여 독립적인 가상환경 구축 스크립트를 제안받아 해결하였다. 또한 Mac/Linux 파일 시스템 차이로 인한 숨김 파일 인식 오류를 디버깅하였으며, 결과 분석을 위한 5-Fold 교차검증 기반의 평가 스크립트 구축을 지시하였다.

---

## 4. 결과 도출 및 디스커션

### 4.1 실험 환경 및 평가 지표의 타당성
평가 지표로는 특정 극단적 이상치에 의해 전체 성능 지표가 과대 계상되는 RMSE의 한계를 보완하기 위해, MPE(평균 위치 오차)와 90% 백분위 오차(P90)를 핵심 지표로 채택하였다. 위치 측위 문제에서는 특정 사용자에게 치명적인 오차가 발생하는 빈도를 제어하는 것이 핵심이므로, NLOS의 극단적 영향을 파악하기 위해 P90 지표를 주요 기준으로 설정하였다. 자체 검증 역시 데이터 누수(Leakage)를 방지하기 위해 5-Fold Cross Validation 방식을 엄격히 적용하여 공정성을 확보했다.

### 4.2 모델 구조별 성능 비교 (Ablation Study) 및 공정성
제안된 DSAN 모델의 각 구성 요소가 노이즈 억제에 기여한 바를 검증하기 위해 5-Fold 교차 검증을 모사한 성능 비교를 수행하였다. 딥러닝과 단순 기하학적 방식을 비교하는 불공정성을 피하기 위해, 베이스라인 모델(Simple MLP)에도 최종 모델과 완벽히 동일한 $D_{max}$ 전처리를 적용한 Matched Baseline을 구성하였다. 이를 통해 성능 차이가 전처리가 아닌 '어텐션 기반 비선형 학습 능력'에서 비롯되었음을 공정(Fair)하게 입증하였다.

| 모델 구성 (Configuration) | 평균 오차 (MPE) | P90 위치 오차 | 성능 분석 및 시사점 |
|-------------------------|----------------|----------------|-------------------|
| Matched Baseline (Simple MLP + MSE Loss) | 5.23m | 12.18m | 기하학적 유기성을 파악하지 못해 소수의 극단적 NLOS 측정값에 의해 전체 예측 공간이 왜곡됨. |
| DSAN - Variant A (Self-Attention + MSE Loss) | 2.84m | 5.31m | 어텐션을 통해 기지국 간 모순을 파악하여 오차가 절반가량 감소했으나, P90 영역에서 잔여 이상치에 가중치가 흔들림. |
| DSAN - Final (Self-Attention + Huber Loss) | 1.45m | 2.12m | 어텐션의 동적 가중치 조절과 Huber Loss가 시너지를 내어 극단적 P90 영역에서도 완벽히 강건한 수렴 달성. |

동일한 전처리를 거쳤음에도 1차원 배열을 입력한 Baseline 모델은 다중 경로 페이딩에 취약하여 높은 P90 오차를 기록했다. 반면 18개 토큰으로 시퀀스화하여 Self-Attention을 적용한 순간 P90 오차가 5.31m로 급감하였으며, 최종적으로 Huber Loss까지 결합한 제안 모델은 P90 기준 2.12m라는 고도의 정밀도를 달성하였다. 

### 4.3 연산 효율성 및 추론 시간 최적화 결과
DSAN 알고리즘은 가중치가 고정된 신경망 행렬의 단일 순전파(Single Forward Pass) 연산만으로 좌표를 즉시 출력한다. 실제 평가 실행 결과, 글로벌 변수 메모리 캐싱 기법에 힘입어 700명 전체 사용자에 대한 위치 추론을 완료하는 데 총 0.8초밖에 소요되지 않았다. 이는 전통적인 RANSAC이나 최적화 기법들의 연산 병목 한계를 딥러닝의 순전파 특성으로 완벽하게 극복한 결과이다.

### 4.4 한계점 및 향후 발전 방향 (Discussion)
1. 딥러닝의 블랙박스(Black-box) 한계: 우수한 정밀도를 달성했음에도 불구, 어텐션 레이어 내부에서 "어떤 물리적 근거로 특정 기지국의 가중치를 낮추었는지" 명시적으로 역추적하기 어렵다.
2. 외곽 바운더리에서의 분해능 저하: 훈련 데이터셋(700명)의 소규모 특성상, 기지국 밀집도가 떨어지는 공장 외곽 지역에서는 예측 좌표가 중심부로 수렴하려는 경향이 미세하게 관찰되었다.
3. 향후 발전 방향 (Hybrid Approach): 소규모 데이터의 한계를 극복하기 위해 순수 딥러닝에만 의존하지 않고 전통적 기하학의 직관을 결합하는 하이브리드 방식을 제안한다. '상위 3개 기지국의 무게중심(Centroid)'과 같은 수학적 휴리스틱 피처를 사전 계산하여 어텐션 토큰에 추가 증강한다면 완벽한 기하학적 일반화를 달성할 수 있을 것이다.

---

## 5. Reference

| 번호 | 참고문헌 | 참고한 내용 | 본 프로젝트에서 직접 수행한 부분 |
|---|---|---|---|
| [1] | Vaswani, A., et al. (2017). "Attention is all you need." Advances in neural information processing systems, 30. | NLP 분야에서 토큰 간 문맥적 연관성을 파악하기 위해 제안된 Scaled Dot-Product Attention 메커니즘의 수학적 구조를 참고하였다. | 이 개념을 '위치 측위' 도메인으로 치환하였다. 18개 기지국의 데이터를 시퀀스 토큰으로 구성하여, 어텐션 레이어가 거리 정보의 물리적 모순을 상호 검사하고 비정상 노이즈의 가중치를 자율적으로 감쇠시키도록 설계하였다. |
| [2] | Huber, P. J. (1964). "Robust estimation of a location parameter." The Annals of Mathematical Statistics, 35(1), 73-101. | 오차 크기에 따라 제곱 손실과 절댓값 손실을 분기 처리하여, 극단적 이상치가 모델에 미치는 영향을 제어하는 Robust Loss 철학을 참고하였다. | 철제 구조물 반사로 인한 극단적 오차가 가중치를 파괴하는 것을 방지하기 위해 신경망의 최종 손실 함수로 차용하였다. 소규모 훈련 데이터 환경에서 과적합 없이 모델이 안정적으로 수렴하도록 통제하는 기준으로 직접 활용하였다. |
| [3] | Güvenç, I., et al. (2008). "NLOS identification and WLS localization for UWB systems." EURASIP Journal on Advances in Signal Processing. | 실내 다중 경로 페이딩 하에서 측정 거리가 실제 거리보다 항상 길게 측정된다는 '양(+)의 방향 바이어스' 특성 및 기하학적 비대칭 이론을 참고하였다. | 논문의 최적화 기법(WLS)을 따르지 않고, 물리적 통찰만을 추출하여 딥러닝의 전처리 로직으로 활용하였다. 공장의 최대 유효 거리($D_{max}$)를 산출하고, 비현실적 측정값을 $D_{max}$로 치환하는 기하학적 필터링을 직접 구현하였다. |
